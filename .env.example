# Azure Communication Services — Email
# Copy to .env.local and fill in real values (.env.local is gitignored).
ACS_CONNECTION_STRING="endpoint=https://<resource>.communication.azure.com/;accesskey=<key>"
ACS_SENDER_ADDRESS="DoNotReply@<verified-domain>"

# Where new lead notifications are emailed (comma-separated)
LEAD_NOTIFY_TO="person1@example.com,person2@example.com"

# BCC'd on every lead notification (comma-separated, optional)
LEAD_NOTIFY_BCC="ops1@example.com,ops2@example.com"
